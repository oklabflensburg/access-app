# Set these environment variables for a non-Docker PHP server.
DB_DSN=pgsql:host=127.0.0.1;port=55432;dbname=accessapp
DB_USER=accessapp
DB_PASSWORD=accessapp-local
# Keep uploaded files outside the public document root.
PHOTO_DIR=/absolute/path/to/accessapp/backend/var/photos
